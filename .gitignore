# .NET build output
bin/
obj/

# IDE
.vs/
.idea/
*.user

# Secrets — copy .env.example to .env and fill in real values, never commit .env
.env
.env.*
!.env.example

# BCKashWebClient keeps its own independent git history/remote — not nested into this repo.
BCKashWebClient/

.DS_Store
